import { Charts } from "components/common";

export default function Chart(): JSX.Element {
  return (
    <main className="h-full w-full border border-gray-300 shadow px-6 py-4 rounded-lg">
      <Charts />
    </main>
  );
}
