import { Charts } from "components/common";

export default function Chart(): JSX.Element {
  return (
    <main className="border border-gray-300 shadow px-6 py-4 rounded-lg h-full w-full">
      <Charts />
    </main>
  );
}
