import { Charts } from "components/common";

export default function Chart(): JSX.Element {
  return (
    <main className="h-fit border border-gray-300 shadow px-6 py-4 rounded-lg w-full">
      <Charts />
    </main>
  );
}
